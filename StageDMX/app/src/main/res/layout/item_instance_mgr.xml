<?xml version="1.0" encoding="utf-8"?>
<!-- 实例行：选中状态用整行背景高亮表示（不用勾选框）。
     点行 = 选中/取消选中（参与同时控制）；长按 = 进入多选删除模式。 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/bg_card"
    android:paddingVertical="10dp"
    android:paddingHorizontal="12dp"
    android:layout_marginBottom="6dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">
        <TextView
            android:id="@+id/tvInstName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text"
            android:textSize="14sp" />
        <TextView
            android:id="@+id/tvInstInfo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/textDim"
            android:textSize="12sp" />
    </LinearLayout>

    <!-- 选中标记（多选删除模式下也用它，文案由代码切换） -->
    <TextView
        android:id="@+id/tvInstCheck"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minWidth="34dp"
        android:gravity="center"
        android:text=""
        android:textColor="@color/ok"
        android:textSize="15sp"
        android:textStyle="bold" />

    <!-- 编辑地址（非多选模式下显示） -->
    <TextView
        android:id="@+id/btnInstEdit"
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:paddingHorizontal="10dp"
        android:gravity="center"
        android:text="@string/s_address"
        android:textColor="@color/accent"
        android:textSize="12sp"
        android:background="@drawable/bg_pill"
        android:clickable="true"
        android:focusable="true" />
</LinearLayout>
